; C++ types query (extends C)

; Template types
(template_type
  (type) @type)

; Auto type deduction
(auto_type) @type

; Decltype
(decltype_specifier
  (type) @type)

; Lambda types
(lambda_expression) @type

; Concept constraints
(concept_constraint) @type

; Requires expressions
(requires_expression) @type

; Template template parameters
(template_template_parameter) @type

; Parameter packs
(parameter_pack) @type

; Fold expressions
(fold_expression) @type